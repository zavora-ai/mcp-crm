server_id = "mcp_crm"
display_name = "Crm"
version = "1.1.1"
domain = "business-systems"
sdk_version = "0.2.0"
risk_level = "high"
writes_allowed = "gated"
transports = ["stdio"]
credentials = ["vault://salesforce-oauth", "vault://hubspot-token", "vault://zoho-crm-token", "vault://pipedrive-token"]
governance_gates = ["crm-write-policy", "mrtr_approval"]
environments = ["development", "staging", "production"]

[[tools]]
name = "list_contacts"
description = "List contacts with optional search query"
risk_class = "read_only"
requires_approval = false
credential_bindings = ["vault://salesforce-oauth", "vault://hubspot-token", "vault://zoho-crm-token", "vault://pipedrive-token"]

[[tools]]
name = "get_contact"
description = "Get a contact by ID"
risk_class = "read_only"
requires_approval = false
credential_bindings = ["vault://salesforce-oauth", "vault://hubspot-token", "vault://zoho-crm-token", "vault://pipedrive-token"]

[[tools]]
name = "create_contact"
description = "Create a new contact"
risk_class = "internal_write"
requires_approval = false
credential_bindings = ["vault://salesforce-oauth", "vault://hubspot-token", "vault://zoho-crm-token", "vault://pipedrive-token"]

[[tools]]
name = "update_contact"
description = "Update an existing contact"
risk_class = "internal_write"
requires_approval = false
credential_bindings = ["vault://salesforce-oauth", "vault://hubspot-token", "vault://zoho-crm-token", "vault://pipedrive-token"]

[[tools]]
name = "search_contacts"
description = "Search contacts by name, email, or custom query"
risk_class = "read_only"
requires_approval = false
credential_bindings = ["vault://salesforce-oauth", "vault://hubspot-token", "vault://zoho-crm-token", "vault://pipedrive-token"]

[[tools]]
name = "list_companies"
description = "List companies/accounts"
risk_class = "read_only"
requires_approval = false
credential_bindings = ["vault://salesforce-oauth", "vault://hubspot-token", "vault://zoho-crm-token", "vault://pipedrive-token"]

[[tools]]
name = "get_company"
description = "Get a company by ID"
risk_class = "read_only"
requires_approval = false
credential_bindings = ["vault://salesforce-oauth", "vault://hubspot-token", "vault://zoho-crm-token", "vault://pipedrive-token"]

[[tools]]
name = "create_company"
description = "Create a new company/account"
risk_class = "internal_write"
requires_approval = false
credential_bindings = ["vault://salesforce-oauth", "vault://hubspot-token", "vault://zoho-crm-token", "vault://pipedrive-token"]

[[tools]]
name = "update_company"
description = "Update an existing company"
risk_class = "internal_write"
requires_approval = false
credential_bindings = ["vault://salesforce-oauth", "vault://hubspot-token", "vault://zoho-crm-token", "vault://pipedrive-token"]

[[tools]]
name = "list_deals"
description = "List deals/opportunities"
risk_class = "read_only"
requires_approval = false
credential_bindings = ["vault://salesforce-oauth", "vault://hubspot-token", "vault://zoho-crm-token", "vault://pipedrive-token"]

[[tools]]
name = "get_deal"
description = "Get a deal by ID with stage and value"
risk_class = "read_only"
requires_approval = false
credential_bindings = ["vault://salesforce-oauth", "vault://hubspot-token", "vault://zoho-crm-token", "vault://pipedrive-token"]

[[tools]]
name = "create_deal"
description = "Create a new deal/opportunity"
risk_class = "internal_write"
requires_approval = false
credential_bindings = ["vault://salesforce-oauth", "vault://hubspot-token", "vault://zoho-crm-token", "vault://pipedrive-token"]

[[tools]]
name = "update_deal"
description = "Update a deal (stage, value, close date)"
risk_class = "internal_write"
requires_approval = false
credential_bindings = ["vault://salesforce-oauth", "vault://hubspot-token", "vault://zoho-crm-token", "vault://pipedrive-token"]

[[tools]]
name = "move_deal_stage"
description = "Move a deal to a different pipeline stage"
risk_class = "internal_write"
requires_approval = false
credential_bindings = ["vault://salesforce-oauth", "vault://hubspot-token", "vault://zoho-crm-token", "vault://pipedrive-token"]

[[tools]]
name = "list_activities"
description = "List activities (calls, emails, meetings, tasks) for a record"
risk_class = "read_only"
requires_approval = false
credential_bindings = ["vault://salesforce-oauth", "vault://hubspot-token", "vault://zoho-crm-token", "vault://pipedrive-token"]

[[tools]]
name = "create_activity"
description = "Log an activity (call, email, meeting, task, note)"
risk_class = "internal_write"
requires_approval = false
credential_bindings = ["vault://salesforce-oauth", "vault://hubspot-token", "vault://zoho-crm-token", "vault://pipedrive-token"]

[[tools]]
name = "list_pipelines"
description = "List sales pipelines and their stages"
risk_class = "read_only"
requires_approval = false
credential_bindings = ["vault://salesforce-oauth", "vault://hubspot-token", "vault://zoho-crm-token", "vault://pipedrive-token"]

[[tools]]
name = "get_pipeline_summary"
description = "Get pipeline summary with deal counts and values per stage"
risk_class = "read_only"
requires_approval = false
credential_bindings = ["vault://salesforce-oauth", "vault://hubspot-token", "vault://zoho-crm-token", "vault://pipedrive-token"]

[[tools]]
name = "list_notes"
description = "List notes attached to a contact, company, or deal"
risk_class = "read_only"
requires_approval = false
credential_bindings = ["vault://salesforce-oauth", "vault://hubspot-token", "vault://zoho-crm-token", "vault://pipedrive-token"]

[[tools]]
name = "create_note"
description = "Add a note to a contact, company, or deal"
risk_class = "internal_write"
requires_approval = false
credential_bindings = ["vault://salesforce-oauth", "vault://hubspot-token", "vault://zoho-crm-token", "vault://pipedrive-token"]

[[tools]]
name = "delete_contact"
description = "Delete a contact by ID"
risk_class = "external_write"
requires_approval = true
credential_bindings = ["vault://salesforce-oauth", "vault://hubspot-token", "vault://zoho-crm-token", "vault://pipedrive-token"]

[[tools]]
name = "delete_deal"
description = "Delete a deal by ID"
risk_class = "external_write"
requires_approval = true
credential_bindings = ["vault://salesforce-oauth", "vault://hubspot-token", "vault://zoho-crm-token", "vault://pipedrive-token"]

[[tools]]
name = "associate_contact_company"
description = "Associate a contact with a company"
risk_class = "internal_write"
requires_approval = false
credential_bindings = ["vault://salesforce-oauth", "vault://hubspot-token", "vault://zoho-crm-token", "vault://pipedrive-token"]

[[tools]]
name = "associate_deal_contact"
description = "Associate a deal with a contact"
risk_class = "internal_write"
requires_approval = false
credential_bindings = ["vault://salesforce-oauth", "vault://hubspot-token", "vault://zoho-crm-token", "vault://pipedrive-token"]

[[tools]]
name = "list_deal_contacts"
description = "List contacts associated with a deal"
risk_class = "read_only"
requires_approval = false
credential_bindings = ["vault://salesforce-oauth", "vault://hubspot-token", "vault://zoho-crm-token", "vault://pipedrive-token"]

[[tools]]
name = "search_companies"
description = "Search companies by name or domain"
risk_class = "read_only"
requires_approval = false
credential_bindings = ["vault://salesforce-oauth", "vault://hubspot-token", "vault://zoho-crm-token", "vault://pipedrive-token"]

[[tools]]
name = "search_deals"
description = "Search deals by name, stage, or value"
risk_class = "read_only"
requires_approval = false
credential_bindings = ["vault://salesforce-oauth", "vault://hubspot-token", "vault://zoho-crm-token", "vault://pipedrive-token"]

[[tools]]
name = "update_activity"
description = "Update an activity (mark done, change subject)"
risk_class = "internal_write"
requires_approval = false
credential_bindings = ["vault://salesforce-oauth", "vault://hubspot-token", "vault://zoho-crm-token", "vault://pipedrive-token"]
